# Claude Code repository guidance

@AGENTS.md

The `.claude/commands/` aliases and `.claude/settings.json` hook are optional
conveniences for this checkout. Core skills and workflows must also work in
other coding agents without these features.
